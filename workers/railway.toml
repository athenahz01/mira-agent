[build]
builder = "DOCKERFILE"
dockerfilePath = "workers/Dockerfile"

[deploy]
startCommand = "node --experimental-strip-types workers/index.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
